[project]
name = "scraper-daemon"
version = "0.1.0"
description = "Reddit scraper daemon for ingesting posts into the WS DB API"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
